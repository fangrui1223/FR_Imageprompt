<Window x:Class="PromptVault.App.LibrarySetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="欢迎使用 FR_Imageprompt" Width="620" Height="390" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Background="{StaticResource BackgroundBrush}">
  <Border Margin="24" Padding="26" CornerRadius="16" Background="{StaticResource PanelBrush}" BorderBrush="#31465E" BorderThickness="1">
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
      <TextBlock Text="创建你的图片提示词图库" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" />
      <TextBlock Grid.Row="1" Margin="0,10,0,22" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap"
                 Text="请选择图库文件夹。原图、缩略图和提示词数据库都会保存在这里，之后可以整体备份或移动。" />
      <TextBlock Grid.Row="2" Margin="0,0,0,7" Foreground="{StaticResource PrimaryTextBrush}" Text="图库位置" />
      <Grid Grid.Row="3">
        <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="90" /></Grid.ColumnDefinitions>
        <TextBox x:Name="PathBox" Height="40" VerticalContentAlignment="Center" />
        <Button Grid.Column="1" Margin="10,0,0,0" Content="浏览" Click="BrowseClick" />
      </Grid>
      <Button Grid.Row="4" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,20,0,0"
              Background="{StaticResource AccentBrush}" Foreground="#061019" FontWeight="Bold" Width="120" Height="42" Content="确认" Click="StartClick" IsDefault="True" />
    </Grid>
  </Border>
</Window>
